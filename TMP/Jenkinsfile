pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout([$class: 'GitSCM', branches: [[name: '*/master']], extensions: [[$class: 'RelativeTargetDirectory', relativeTargetDir: 'C:\\Users\\mc\\JenkinsPipeline'], [$class: 'CleanBeforeCheckout', deleteUntrackedNestedRepositories: true]], userRemoteConfigs: [[credentialsId: 'f563abd0-ab5c-4ec8-b434-4dfe75a8bf7d', url: 'https://Rukpong@bitbucket.org/arcadiasoftware/arcadia.automate.buffet.git']]])
            }
        }
    }
}
